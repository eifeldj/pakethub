# Dashboard card

```yaml
type: custom:pakethub-card
title: My packages
show_delivered: false
max_packages: 8
sort_by: status
tap_action: details
```

Supported tap actions:

- `details`
- `url`
- `more-info`

The resource is registered automatically for storage-mode dashboards.
